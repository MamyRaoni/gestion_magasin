{% extends 'base.html.twig' %}

{% block title %}CommandeProducts{% endblock %}

{% block body %}
    <h1>CommandeProducts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_product.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ commande_product.quantity }}</td>
            </tr>
            <tr>
                <th>UnitPrice</th>
                <td>{{ commande_product.unitPrice }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_products_index') }}">back to list</a>

    <a href="{{ path('app_commande_products_edit', {'id': commande_product.id}) }}">edit</a>

    {{ include('commande_products/_delete_form.html.twig') }}
{% endblock %}
